Config = {}

-- Options: 'male' or 'female'
Config.DefaultGender = 'male'

-- false = only forces freemode on first spawn if player is not already freemode
-- true = forces freemode every spawn/respawn
Config.ForceEverySpawn = false

-- Command to manually switch back to freemode
Config.Command = 'freemode'